'use client'
import { FormEvent,useState } from 'react'; import { useRouter } from 'next/navigation'; import Link from 'next/link'
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false),router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();setLoading(false);if(!r.ok)return setError(d.error);router.push(d.isAdmin?'/admin':'/')}return <main className="grid min-h-screen place-items-center bg-[#f5f1ed] p-5"><div className="w-full max-w-md rounded-xl bg-[#fffbf7] p-9 shadow-xl"><Link href="/" className="font-serif text-2xl">Morrow & Wick</Link><h1 className="mt-12 font-serif text-5xl">Admin login</h1><p className="mt-3 text-sm text-[#6e6760]">Manage products, pricing, and inventory.</p><form onSubmit={submit} className="mt-8 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full rounded-lg border bg-transparent px-4 py-3 outline-none"/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-lg border bg-transparent px-4 py-3 outline-none"/>{error&&<p className="text-sm text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-[#292724] py-4 text-white">{loading?'Signing in…':'Sign in'}</button></form><Link href="/" className="mt-6 block text-center text-sm text-[#8b8178]">Return to storefront</Link></div></main>}
